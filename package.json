{
  "name": "bilinguismo-project",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "shared"
  ],
  "scripts": {
    "client": "cd client && npm run dev",
    "build:client": "cd client && npm run build",
    "dev": "npm run client"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
